# Security

Collectors may enforce API key, mTLS, JWT, rate limits, and request body size constraints.

Security controls are deployment-specific, but they must not silently mutate canonical event identity.
