# Wire Formats

Supported contract-level wire formats:

- JSON object
- JSON array
- wrapped JSON payload
- NDJSON
- gzip-compressed equivalents

Protocol Buffers may be added later if there is a clear runtime need.
