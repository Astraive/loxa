# SDK Config

SDK config controls:

- service identity
- event schema
- sinks
- async queueing
- duplicate policy
- security limits
- samplers
- redactors

Use presets:

- `loxa.Dev()`
- `loxa.Production()`
- `loxa.Test()`
