# Raw Preservation

Collectors must preserve the original incoming event bytes by default.

Validation must not rewrite producer identity fields.

If normalization is enabled, normalized output should be stored separately from the original raw payload.
