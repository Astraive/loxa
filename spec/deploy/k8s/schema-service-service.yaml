apiVersion: v1
kind: Service
metadata:
  name: loza-schema-service
  labels:
    app.kubernetes.io/name: schema-service
    app.kubernetes.io/part-of: loza
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: schema-service
    app.kubernetes.io/part-of: loza
  ports:
    - name: http
      port: 9315
      targetPort: http
