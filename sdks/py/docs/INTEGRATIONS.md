# Integrations

Implemented Python integration modules:

- `loxa.integrations.logging`
- `loxa.integrations.loguru`
- `loxa.integrations.structlog`
- `loxa.integrations.otel`

Go-specific integration names such as `slog`, `zap`, and `zerolog` are not shipped as Python modules.
