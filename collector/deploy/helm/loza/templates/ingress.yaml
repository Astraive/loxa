{{- if and .Values.collector.enabled .Values.collector.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "loza.fullname" . }}-collector
  labels:
    {{- include "loza.labels" . | nindent 4 }}
    app.kubernetes.io/component: collector
  {{- with .Values.collector.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.collector.ingress.className }}
  ingressClassName: {{ .Values.collector.ingress.className }}
  {{- end }}
  {{- if .Values.collector.ingress.tls }}
  tls:
    {{- range .Values.collector.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.collector.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "loza.fullname" $ }}-collector
                port:
                  number: {{ $.Values.collector.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
