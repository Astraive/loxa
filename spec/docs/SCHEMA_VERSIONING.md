# Schema Versioning

- `schema_version` identifies the contract line for canonical field expectations
- `event_version` identifies event-shape evolution within the contract
- new optional fields are additive
- required field removals or type changes are breaking
