apiVersion: apps/v1
kind: Deployment
metadata:
  name: loxa-schema-service
  labels:
    app.kubernetes.io/name: schema-service
    app.kubernetes.io/part-of: loxa
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: schema-service
      app.kubernetes.io/part-of: loxa
  template:
    metadata:
      labels:
        app.kubernetes.io/name: schema-service
        app.kubernetes.io/part-of: loxa
    spec:
      containers:
        - name: schema-service
          image: ghcr.io/astraive/loxa-schema-service:0.2.5
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - >-
              python -u services/schema_service.py
              --host ${SCHEMA_SERVICE_HOST:-0.0.0.0}
              --port ${SCHEMA_SERVICE_PORT:-9315}
          ports:
            - name: http
              containerPort: 9315
          envFrom:
            - configMapRef:
                name: loxa-schema-service-config
            - secretRef:
                name: loxa-schema-service-secrets
                optional: true
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
