name: Loxa Spec Rollout

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/loxa-spec-rollout.yml"
      - "docker-compose.yml"
      - "loxa-spec/**"
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/loxa-spec-rollout.yml"
      - "docker-compose.yml"
      - "loxa-spec/**"

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r loxa-spec/requirements-service.txt

      - name: Run stager and Kafka adapter tests
        run: python -m unittest loxa-spec.tests.test_stager loxa-spec.tests.test_stager_resilience loxa-spec.tests.test_kafka_adapter

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Render Helm templates
        run: helm template loxa ./loxa-spec/charts/loxa --namespace loxa

      - name: Build schema-service image
        run: docker build -f loxa-spec/docker/schema-service/Dockerfile -t loxa-schema-service:test loxa-spec

      - name: Build stager image
        run: docker build -f loxa-spec/docker/stager/Dockerfile -t loxa-stager:test loxa-spec
