# Duplicate Fields

Producers should avoid emitting custom attributes that collide with canonical top-level fields.

SDKs and collectors may expose duplicate-field policies, but the canonical names defined by this repository remain reserved.
