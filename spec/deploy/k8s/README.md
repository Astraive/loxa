Thin example manifests for `schema-service` and `stager`.

The canonical deploy source now lives in `spec/charts/loza`.
Keep these manifests aligned with the chart outputs or regenerate them from the chart when packaging examples.
